// testing print statements of multiple types both individually as well as on one line


x : integer = 19;

test : function integer (str : string);

main : function integer (argc : integer, argv : array [] string) = {
    print test("this is a string literal");
    return 0;
}

test : function integer (str : string) = {
    print str, "\n";
    return 3;
}

/*
.arch armv8-a
.file   "output.s"

.data
        .global x
x:      .word 19
.text
        .global main
        main:
                stp     x29, x30, [sp, #-200]!
                str     x0, [sp, 16]
                str     x1, [sp, 24]
                adrp    x9, .LT1
                add     x9, x9, :lo12:.LT1
                mov     x0, x9
                bl      test
                mov     x9, x0
                mov     x0, x9
                bl      print_integer
                mov     x9, #0
                mov     x0, x9
                ldp     x29, x30, [sp], #200
                ret
                ldp     x29, x30, [sp], #200
                ret
        .section        .rodata
.LT1:
        .string "this is a string literal"
.text
        .global test
        test:
                stp     x29, x30, [sp, #-200]!
                str     x0, [sp, 16]
                ldr     x10, [sp, 16]
                mov     x0, x10
                bl      print_string
                adrp    x10, .LT2
                add     x10, x10, :lo12:.LT2
                mov     x0, x10
                bl      print_string
                mov     x10, #3
                mov     x0, x10
                ldp     x29, x30, [sp], #200
                ret
                ldp     x29, x30, [sp], #200
                ret
        .section        .rodata
.LT2:
        .string "\n"
*/