// testing print statements of multiple types both individually as well as on one line


str2 : string = "two ";

test : function integer (str1 : string, str2 : string, str3 : string);

main : function integer (argc : integer, argv : array [] string) = {
    str1 : string = "one ";
    test(str1, str2, "three ");
    return 0;
}

test : function integer (str1 : string, str2 : string, str3 : string) = {
    print str1, str2, str3;
    return 3;
}

/*
.arch armv8-a
.file   "output.s"

.data
        .global str2
str2:   .string "two "
.text
        .global main
        main:
                stp     x29, x30, [sp, #-200]!
                str     x0, [sp, 16]
                str     x1, [sp, 24]
                adrp    x9, .L2
                add     x9, x9, :lo12:.L2
                mov     x0, x9
                adrp    x9, str2
                add     x9, x9, :lo12:str2
                mov     x1, x9
                adrp    x9, .LT1
                add     x9, x9, :lo12:.LT1
                mov     x2, x9
                bl      test
                mov     x9, x0
                mov     x9, #0
                mov     x0, x9
                ldp     x29, x30, [sp], #200
                ret
                ldp     x29, x30, [sp], #200
                ret
        .section        .rodata
.L2:
        .string "one "
        .section        .rodata
.LT1:
        .string "three "
.text
        .global test
        test:
                stp     x29, x30, [sp, #-200]!
                str     x0, [sp, 16]
                str     x1, [sp, 24]
                str     x2, [sp, 32]
                ldr     x9, [sp, 16]
                mov     x0, x9
                bl      print_string
                ldr     x9, [sp, 24]
                mov     x0, x9
                bl      print_string
                ldr     x9, [sp, 32]
                mov     x0, x9
                bl      print_string
                mov     x9, #3
                mov     x0, x9
                ldp     x29, x30, [sp], #200
                ret
                ldp     x29, x30, [sp], #200
                ret
*/