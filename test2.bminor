// testing a simple program with integer arithmetic, global vars, local vars, expressions, multiple scopes, and print statements


str : string = "LET'S GOOOOOO!!!!";

main : function integer (argc : integer, argv : array[] string) = {

    print str;
    return 0;
}

/*
.arch armv8-a
.file   "output.s"

.data
        .global str
str:    .word "LET'S GOOOOOO!!!!"
.text
        .global main
        main:
                stp     x29, x30, [sp, #-200]!
                adrp    x9, str
                add     x9, x9, :lo12:str
                mov     x0, x9
                bl      print_string
                mov     x9, #0
                mov     x0, x9
                ldp     x29, x30, [sp], #200
                ret
                ldp     x29, x30, [sp], #200
                ret
*/