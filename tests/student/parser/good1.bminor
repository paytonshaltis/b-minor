// testing function prototypes and implementations, function calls within functions


proto1 : function char ();                                                              // function prototypes with and without parameters
proto2 : function void (x : integer);
proto3 : function string (arr : array[] array[] string, size : integer);

power : function integer (base : integer, exp : integer) = {                            //function declarations with and without parameters
    /* purposefully more complex than it needs to be for more testing! */
    temp1 : integer = base;
    temp2 : integer = exp;
    temp3 = temp1 ^ temp2;
    return temp3;
}
intAt : function integer (a : array[] integer, location : integer) = {
    return a[location];
}

main : function integer (argc : integer, argv : array[] string) = {
    num1 : integer = 5;
    arr : array[2] integer = {1, 2};
    num2 : integer = power(2, num1) + intAt(arr, 1) + power(var1 * var2, var3 % var4);
    
    return 0;
}